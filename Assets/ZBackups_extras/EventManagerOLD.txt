using UnityEngine;
using System.Collections;

		/*
		foreach (Object obj in objects) {
			//check collision / proximity to m2
			Debug.Log (obj.name);
		}
		
		foreach (Object door in doors) {
			//check collision / proximity to m2
			Debug.Log (door.name);
		}
		*/

public class EventManager : MonoBehaviour 
{
	public delegate void ClickAction();
	public static event ClickAction OnClicked;
	private string thisString = "Some long text description";
	//private Vector2 textDimensions;
	//private float labelX;
	//private float labelY;
	//private float offset = 1.5f;
	private GUIStyle myStyle = new GUIStyle();

	//public GUIStyle style = null;
	//private Font thisFont = (Font)Resources.Load("LcdStd", typeof(Font));

	void Start() {
		Debug.Log ("HEY lookie me :D");
	}

	void OnGUI() {
		Font thisFont;
		thisFont = Resources.Load<Font> ("LcdStd");
		//thisFont = Resources.Load<Font> ("PhoenixScript");
		//thisFont = Resources.Load<Font> ("MAGNETOB");
		//thisFont = Resources.Load<Font> ("Japonesa_0");
		
		myStyle.font = thisFont;
		myStyle.fontSize = 40;
		myStyle.normal.textColor = Color.white;
		myStyle.alignment = TextAnchor.MiddleCenter;
		//GUI.skin.GetStyle (myStyle);
		
		//textDimensions = GUI.skin.label.CalcSize(new GUIContent(thisString));
		//labelX = textDimensions.x;
		//labelY = textDimensions.y;

		GUI.Label (new Rect (Screen.width / 2 - 50, 10, 100, 30), thisString, myStyle);
		Debug.Log (GUI.skin.GetStyle("Label"));

		if(GUI.Button(new Rect(Screen.width / 2 - 50, Screen.height - 50, 100, 30), "WOOT")) 		{
			if(OnClicked != null)
				OnClicked();
			}
		}

	//public GUIText instruction;

	//instruction.text = "Blast Off!";

}
